git init

git status

git add

git commit -m ""

git log

//for rolling back to checkpoints
git diff filename

git checkout filename

//for pushing it to github repo:

git remote add origin //link in ur new repo

git branch //to check out ur cuurent local branch

git push -u origin master //push into a remote repository

